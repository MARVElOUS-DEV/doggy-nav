{
  "name": "doggy-nav-core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared domain types, services and repository interfaces for doggy-nav.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json -w",
    "test": "pnpm build && node src/services/AiRecommendationService.test.mjs && node src/services/AiProviderService.test.mjs && node src/services/ToolOutputPublicationService.test.mjs"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5"
  }
}
