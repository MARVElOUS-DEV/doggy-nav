# Example environment file for Docker deployment
# Copy this file to .env and update the values for your deployment

# JWT Secret - REQUIRED: Generate a strong random secret for production
# This must be at least 32 characters and should be cryptographically secure
JWT_SECRET=your-super-secure-jwt-secret-here-change-in-production

# MongoDB Connection - Optional: Override if using external MongoDB
# Leave commented to use the MongoDB container defined in docker-compose
# MONGO_URL=mongodb://username:password@your-mongo-host:27017/doggy-nav

# Application Environment
NODE_ENV=production
EGG_SERVER_ENV=prod

# Optional: Additional configuration
# PORT=3002